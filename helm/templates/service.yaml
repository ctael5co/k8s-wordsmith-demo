apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s-wordsmith-demo.fullname" . }}
  labels:
    app: {{ include "k8s-wordsmith-demo.name" . }}
    chart: {{ include "k8s-wordsmith-demo.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "k8s-wordsmith-demo.name" . }}
    release: {{ .Release.Name }}
